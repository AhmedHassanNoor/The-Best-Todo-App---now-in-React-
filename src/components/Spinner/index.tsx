import React from "react";
import "./styles.css";

//For this component and it styles, I have shamelessly invoked the magic of W3Schools to copy paste the code!
const CustomLoader: React.FC = () => {
  return <div className="loader"></div>;
};

export default CustomLoader;
